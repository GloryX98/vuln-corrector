# Normalize line endings; silences the LF/CRLF warnings on Windows.
* text=auto eol=lf
# Keep Windows scripts as CRLF, shell scripts as LF (so build.sh runs on *nix).
*.ps1 text eol=crlf
*.bat text eol=crlf
*.sh  text eol=lf
# Treat the binary as binary.
*.exe binary
